import { StyledBasePageContiner } from '../../BaseStyledComponents/BaseStyled';

//reset db and remove by id
const Remove = () => {
    return <StyledBasePageContiner>remove</StyledBasePageContiner>;
};

export default Remove;
