import styled from 'styled-components';
import { StyledBaseDiv } from '../../BaseStyledComponents/BaseStyled';

export const PackagesContainer = styled(StyledBaseDiv)`
    display: flex;
    flex-direction: column;
    gap: 4rem;
    margin-bottom: 100px;
`;
