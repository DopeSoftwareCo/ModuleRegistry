import styled from 'styled-components';
import { StyledBaseDiv } from '../../BaseStyledComponents/BaseStyled';

export const IconsContainer = styled(StyledBaseDiv)`
    display: flex;
    gap: 0.2rem;
`;
